# .claude/tests

Gameplay-driving test suites that are worth keeping.

Most games in this repo are verified by a drive suite written in the session
scratchpad and thrown away afterwards — that is still the default, and it is
fine for a game whose rules fit in your head. A suite earns a place here when
**re-deriving it would cost more than reading it**: when it encodes fairness or
design invariants rather than just poking the UI.

## Rules a check has to clear

Three of these were learned the hard way on 2026-08-24, writing the
star-surge title-screen group. All three describe checks that were **green and
worthless**, which is the only really dangerous kind.

- **A flaky gate is worse than no gate.** A containment check on a procedural
  dogfight passed, then failed on a re-run — 2 escaped samples in 18 000,
  because the spawn is random. The fix is never to loosen the threshold until
  it stops going red: it is to make the property a **hard invariant**. There,
  soft steering springs were doing the flying and the check was measuring luck;
  adding a hard clamp at a limit no player can see turned "never leaves the
  arena" from a probability into something the code guarantees. A gate that
  goes red one run in five is a gate that gets ignored.
- **`> 0` is not a check.** "The fight resolves — a downed hull comes back" was
  written as `respawns > 0`, and it passed while the guns fired down the nose
  and hit essentially nothing (98 shots and zero kills over a simulated
  minute). Sample the real behaviour across a handful of runs, then put the
  threshold **near the observed floor with margin** — `respawns >= 2` against
  an observed 4–8. A bound at the vacuous end of the range asserts that the
  feature exists, not that it works.
- **Stages inside one check must not share mutable state.** An erase-gesture
  check ran three stages — tap, slide-off, full hold — against one seeded
  pilot. When stage 1 regressed, that pilot was already gone, so stage 2's
  press landed on an empty slot instead of the bin and the whole suite died on
  a null. A failure became a crash, which hid the other 76 results and the
  failing check's own name. Re-seed per stage; the cost is three lines.

Two more were learned on 2026-08-25, writing the star-surge 3D-models group —
both are ways a **pixel check** goes green for the wrong reason:

- **The bar must clear what the scene can mimic.** "The dogfight projects
  real hulls" passed with the painter completely dead, because a flat
  4-lit-pixels bar was cleared by stray starfield pixels behind the sample
  box. If the subject sits over a scene, ask what that scene alone scores in
  the box and put the bar well above it — as a *fraction of the box* (5% of
  samples there), never a flat count, so it survives a resize.
- **Cap a resource at every source that can grant it, and assert them in ONE
  check.** Star-surge's shield cap had three grant sites — armor regen, the
  pickup, and a run restored from a save written before the cap existed — and a
  cap added at only one of them is the entire failure mode. One check that
  drives all three fails loudly (5 / 7 / 9 against a cap of 2) where three
  separate checks invite one to be quietly forgotten when a fourth source
  appears.
- **A pixel check should paint its subject ALONE, over the real backdrop.**
  Two of star-surge's shield checks were green with every style's branch
  collapsed into one, because the sample box's radius differed per style — so
  the box moved between styles and could land on the hull. Calling the painter
  directly onto a cleared canvas removes the confound. Then fill the actual
  background colour first: `getImageData` returns **unpremultiplied** RGB, so a
  1.6%-alpha fill over a transparent canvas reads as its full colour and the
  profile comes back flat and bright.
- **Distinct-hash checks are only as good as the region they hash.** "Four
  styles, four distinct pickups" passed with the mesh branch dead, because the
  letter drawn on top was styled per style *outside* the branch. Prefer an
  assertion about behaviour — "the mesh pickups tumble and the flat ones hold
  still" — which a fall-through cannot fake.
- **Negative controls go stale when the art moves — re-fire them after any
  visual retune.** "Banking changes the lighting" measured left-flank vs
  right-flank, and its control (kill the lighting) legitimately failed the
  check when written. A brightness retune *hours later* widened the per-face
  grime spread's visible effect, and the re-fired control passed the check —
  the grime difference between the two flanks now supplied the asymmetry the
  light was supposed to. The fix was structural, not a threshold: compare the
  SAME faces under mirrored poses, so the confound is constant and only the
  guarded mechanism can move the number. A control run once at birth proves
  the check discriminated *then*; after the visuals it measures change, that
  proof has expired.

Two more from 2026-08-27, re-tuning Ember Depths' economy — both are about a
check that is **correct today and becomes noise tomorrow**:

- **Read the constant, never restate it.** Five checks went red the moment the
  gear ladder was re-priced, none of them because anything broke: they had
  `500 - 40 - 40` written into them. A tuned number *is going to move* — that is
  what tuning means — and a check that hard-codes one fails the next balance
  pass for a reason that has nothing to do with what it guards, which trains
  everyone to edit the number and move on. Read it out of the game's own table
  (`GEAR[k].tiers[t].cost`, `CONSUMABLES[k].cost`) and assert the *relationship*:
  "buying debits exactly the price and raises one tier" survives any re-tune,
  because the debit is the rule and the price is data. Where the shape of the
  table is itself a design decision, assert **that** separately and directly —
  "six rungs above a free kit on every track, each dearer than the last" is a
  real invariant a flat rung would violate.
- **Inverting a rule does not invert its check for free.** Ember Depths' auto
  path stopped avoiding traps, so "the path routes around a trap" had to become
  "the path walks over it". The naive flip is **green with the change reverted**:
  a trap on a cell with no way round is crossed by any pathfinder, trap-aware or
  not. The old check's setup work — find a trap cell that sits ON the route *and*
  has a detour available — is exactly what still makes the new one discriminate,
  and it has to be carried across rather than simplified away because the
  assertion got shorter. Re-fire the negative control after any inversion; the
  proof that a check discriminated does not survive the rule changing sign.

And one older rule that keeps earning its place: **break it to believe it.**
Every check above was verified by deliberately breaking the thing it guards and
confirming that check — and only that check — went red. `.claude/scripts/negtest.sh`
exists so the break always gets restored (`save` before, `restore` after, and
note that `restore` consumes the snapshot, so re-`save` before the next break).

| Suite | Game | Why it lives here |
| --- | --- | --- |
| `drive-wayfinder.cjs` | `games/wayfinder/` | 82 checks including a BFS walkability gate, a final-leg gate (every route the lessons instruct is walkable), and a cheat gate (each lesson refuses to complete without its technique). These caught an objective placed behind a 74° face and another placed inside a stream channel. |
| `drive-grid-defense.cjs` | `games/grid-defense/` | 50 checks on RULES (balance lives in the eval next to it, so a rebalance never fights a mechanics regression): the wave-by-wave intro schedule, the timer-driven continuous flow (grace → wave → gap → level clear, ten waves to a level, nothing waiting on a tap), level retry with a whole-attempt rollback so a level cannot be farmed for cores by dying on purpose, tree gating, persistence, scoreboard, and the curve's shape. It also presses the canvas-drawn UI through real touch events, which is where this repo's stale-hitbox bug lives. Caught a clear-phase that re-fired and farmed the level bonus forever, a between-levels auto-save that reopened the next level holding the previous level's board, random elite stamping that made wave HP dip level-to-level, and a deferred game-over screen that popped over a freshly started run. |
| `eval-grid-defense.cjs` | `games/grid-defense/` | Balance and pacing, not rules. Plays the real campaign with declarative strategy personas — no skills, bad skills, bad tower mix, bad placement, no armory, and **one per skill tree** — and asserts the design's difficulty *claims*, including the hard requirement that **specialising in any single tree can still clear all ten levels**. Seeded (`--seed`) because crit rolls alone swung a verdict by two levels between identical runs. It caught: difficulty running backwards inside a level, research strictly dominating the armory, a 23-strategy agent exploration whose headline "3.3x gap" was really short skill lists stranding 88 of 116 points, and two trees that could not finish alone (ENGINEERING after deploy caps gutted its economy identity, COMMAND for having no way to turn army size into damage). |
| `drive-turret-builder.cjs` | `games/turret-builder/` | 207 checks on RULES. The headline is the CD's own worked example — ELEC+BLAST+FIRE+ICE, one per side — asserted **to the decimal off a damage ledger** (`__TB.setLog`) fired at pinned dummies with known armour and resist: 12.5 kinetic and 9.375 burn on the main target, 6.25 and 4.6875 on the arc target, 1.25 and 0.9375 on a bystander, full chill on all three. Then each module alone and stacked four deep, the damage-decays/effects-don't split, continuous tracking and instant snap, all four diagonal boosters (including the CD's claim that two BLAST plus a TWIN transfers a full 100%), all fifteen named combos with their effects and their rotation-invariance, the persistent discoveries codex, walls, economy, flow, the sawtooth, and the canvas UI pressed through real touch events. All **five graphics styles** too — TOON is the default for a first-time visitor, the dropdown ticks the current one, the pick survives a reload, and each renderer is driven through REAL frames on a populated board, because a per-style throw inside `draw()` ends the rAF chain and freezes the game. That set discriminates: breaking `drawGroundNeon` fails only the NEON check. Two more checks guard the skin system itself. One fingerprints each cel skin's turret **silhouette** and requires every pair to differ by at least 3% of samples — written that way because "all four hashes are distinct" passed with `turret: drawTurretMech` deliberately unregistered (the palette still went through), and a silhouette hash alone then passed on a **one-sample** antialiasing difference; the closest honest pair is 7.9% and a fallen-through skin is 0.1%. The other paints one **disc** through `celShape()` inside a rotated frame (`paintCelProbe`), because the turret-tracking sun measurement only bites on a skin that swings a large lit body — STEAMPUNK's thin cannon and STONE AGE's thin arm both pass a ring through geometry that never rotates — and a disc is rotation-invariant, so its bright bearing can only move if the light does. Then a **road group**, all of it measured off pixels: the road's walk order is a one-cell chain with no repeats, its two ends are exactly the cells `canPlace('wall', …)` refuses, every road-cell boundary is struck darker than the cell centre (the CD's "align the lines with the grid so I can see where a wall goes", stated as a measurement — the joints used to be spaced along the path and permanently out of phase), both end cells carry an arrow the plain road does not, and every buildable cell is green at five sample points in every cel style (five, not one: a dirt verge hugging the road misses every cell CENTRE while browning the quarter of each neighbour nearest it, and the first version of that check stayed green against exactly the thing it was written to catch). And the one that makes "a skin is paint" an assertion rather than a claim: the identical deterministic scenario — three module-ringed turrets, a wall, 26 seconds of a live wave — played out under **all five styles with real frames drawn between ticks**, requiring every gameplay number (creep distance, HP, chill, turret aim to six places, wall HP, cash, lives, links) to come out byte-identical. Drawing between the ticks is the whole point: a check that only stepped the sim could not see a draw function writing to a creep, caching onto a tile, or moving the board geometry, which is the failure a large skin system actually invites. It discriminates — `c.d += 0.001` inside `drawCreepMech` fails it and names MECH. Its own first version failed on four styles for a reason that had nothing to do with skins: it put the tracked creep's **id** in the snapshot, and `creepSeq` is a page-lifetime counter no run reset touches, so later styles number their creeps higher. Every one of those six was verified by breaking it. All of them first failed for the same wrong reason — they were reading a `LEVEL 1 · SUBSTATION` toast painted over the board — which is why `clearToasts()` exists. And the **placement rules** — a module must touch a turret or wall, a booster a turret diagonal, wall-only ARMOR/REGEN a wall — with the overlay's mask asserted as the same set `place()` accepts, every cell classified exactly once, and the overlay driven through real frames for every kind and mid-drag. That group discriminates too: reverting `canPlace` to the old anything-anywhere rule fails 11 checks and leaves the mask-consistency ones green, which is right — they assert coherence, not the rule. One check measures **pixels**: the cel rim-light must stay fixed in screen space while a turret rotates to track, sampled as the bearing of the brightest pixel on a ring inside the hull across five aim angles (stubbing `frameRot()` to 0 takes the spread from 0° to 90° — it caught a live defect on 2026-08-23 that no amount of looking at the game had). Caught a booster link crashing the turret bloom (`l.from.mod` is undefined for a booster), a codex that diverged from disk after a storage wipe, and dummies with no speed that silently made every wall test vacuous. |
| `eval-turret-builder.cjs` | `games/turret-builder/` | Balance and pacing, not rules. 22 claims, played with declarative personas — bare turrets, a player who keeps trying to orphan modules (the board now refuses every one, so the persona ends a campaign having wired nothing — the placement rule measured end-to-end rather than in a unit check), a grid that refuses to share, all-in on each of the five modules, boards built entirely of named combos, no boosters, no walls, no lab, exit-camping. It asserts the CD's two design principles directly: **all-in on a damage type carries a run** (increasing returns) and **a combo board is competitive with the best stacked board** — while also pinning the honest counterparts, that **all-ICE cannot carry a run** (a multiplier with nothing to multiply) and **not every combo is good** (CRYO SHELL chills beautifully and kills slowly). It produced the findings that shaped both passes: bare gray triangles reached level 6 until armour was made to scale with the campaign floor; the useful `HP_LEVEL` band after the payload redesign is only ~10% wide (1.30 clears, 1.40 stalls the reference build at 5); and chill measurably stretches a level to ~11 minutes, which is capped so it cannot get worse. Also the meta-lesson: for an hour *no value of `HP_LEVEL` changed any outcome*, because the persona settled for the cheapest affordable lab tier instead of saving for its priority. **If a curve sweep does nothing, the curve is not what is stopping the persona.** |
| `drive-neon-clash.cjs` | `games/neon-clash/` | 67 checks on the rules and the four design promises that are easiest to break silently. The first is **the bunker protects its garrison**: an enemy parked against it must chew the *building* while the occupants' HP never moves, a garrisoned melee unit must fire past its own 6-unit reach through the slits, and when the bunker dies both occupants must come out **alive and mobile** — turning that into a coffin would still pass every other check in the file. The second is the **rotated two-player tray**: two touch identifiers, one starting in each tray, dragged and released together, must deploy one unit per side and charge each side only for its own card, while the same top-tray drag in vs-AI mode must do nothing at all. Around those: the deploy contract from both sides (own half only, pay or fail, a refused deploy costs nothing, a drop on the halfway line clamps inward), the bunker and garrison caps, the stat *shape* the design asks for (tank walls, fighter rushes, archer reaches) rather than its exact numbers, energy at 1/sec capped at 20 and doubled in sudden death, a razed base ending the match, that the AI plays cards without ever spending energy it does not have, and the **portrait lock** — a landscape viewport must keep the portrait layout, and a touch whose screen coordinates are pushed through the view transform must still hit the card it visually covers, which is the check that fails the moment a pointer handler reads `getBoundingClientRect().left` instead of un-rotating. The third promise is **fireball is a comeback card, not a win condition**: its damage must measure 90 dead centre and exactly 45 at the rim (the falloff *is* the balance — flatten it and the card stops rewarding you for reading a crowd), it must leave anything outside the circle untouched, never hurt your own units, never scratch **either** base, and never reach a garrison through the bunker sheltering it; the knockback must actually shove a unit backwards against its own march, a spell dropped on your own bunker must cast rather than garrison, a unit must still land `DRAG_OFF` ahead of the finger while a spell lands exactly under it, and LEGEND must answer a massed push with a cast of its own — the check that proves the AI reaches the spell through the same `tryDeploy` the player uses. The fourth is the **siege lock** — a unit that has landed a blow on a building commits to it until it falls — and it is pinned from *both* directions, because only one of them is the interesting failure: a committed attacker must ignore a defender that walks up beside it (and keep chewing the building meanwhile), while a unit merely *marching* at a base, which has committed to nothing, must still divert onto that defender. Collapsing those two into one rule in either direction reads as a working game and quietly deletes the counterplay the lock exists to create; a third check kills the besieged bunker and requires every attacker it held to be released and re-target. |
| `drive-phasic.cjs` | `games/phasic/` | 246 checks: scripted player solutions for all 24 authored curriculum levels, solver-script replays for every generated level below 65 plus endless spot checks (a served level is a solved level), template-coverage and in-path weave gates (void under the pour line, mid-field hedge, ridden fan lane), base-state rules (born-liquid needs a latched frost; removing it melts again), obstacle gates (void consumes + retry, bush stops stone/drinks liquid/passes vapor, fan blows only gas), the Stopper tactic both ways, symmetric reversion, live sockets, win-with-deployed-well, STUCK, menu format, and a complexity-ramp assertion. Earlier rounds caught freeze teleporting a puddle mid-air, liquid too flat to refreeze (missing cohesion), gas unable to find a flue (missing pressure guidance), and a stale home flag on melted gems that could have faked a win. |
| `drive-star-surge.cjs` | `games/star-surge/` | 98 checks pinning the two-axis weapon model (permanent XP tier = quality; in-run pip 1-4 = quantity/area, **never** fire rate): every weapon's `weaponCooldown()` is pip-invariant; blaster/missiles/bolas fire exactly `[1,2,3,4]` projectiles across pip; bombs alternate turret-count and blast-radius (`BOMB_UPGRADES`); beam/flame band/lobe counts track pip 1:1; chain hits exactly `[1,3,5,7]` enemies (zero jumps at pip 1, +2/pip); EMP radius grows with pip while damage doesn't; each missile tier's effective turn rate implies exactly its `MISSILE_MIN_TURN_RADIUS[tier]` floor; enemy hp and incoming ship damage both rise with `campaignDifficulty()`; plus a crash-free smoke pass per weapon. Seven more cover the ⚙ graphics-style setting: 3D animlight is the default with nothing stored, the dropdown offers exactly the five known styles, the pick updates the live renderer *and* persists, the cog sits clear of ← and 🔊 and its panel closes on the first canvas touch, and — the two that actually earn their place — **every style paints a frame holding all four enemy types, both boss kinds, every projectile shape and a shielded ship at once** (a per-style render branch that only throws on one enemy type is invisible until that wave arrives), and **cel shading keeps the sun fixed in screen space while a sprite spins**, measured by walking a ring inside the spinner’s dome and asserting the brightest bearing does not move with `e.ang`. That one discriminates: stubbing `frameRot()` to 0 takes the spread from 0° to ~170°, which is exactly the "sprite drags its own shadow round" tell that no other check would notice. Written after this exact model was violated once (a pip-based fire-rate bonus that fought the tap-fast-vs-hold-steady player-skill mechanic, reverted the same session) and the missile floor was added to close a "fill the screen with missiles" exploit — see `.claude/star-surge.md`'s Lessons learned for both. Nineteen more cover the **sector report → launch → allied station** loop added 2026-08-23. The ones that earn their place are the structural invariants, not the pixels: **COMBAT resumes the SAME run** (score, XP, salvage, weapon pip and hull all carry through the station — a future refactor reaching for `startGame()` here would silently zero a player's score mid-campaign and no other check would notice); **the two currencies stay unconvertible** (repair debits credits and never XP, shipyard unlocks debit XP and never credits); **every callout points at a feature on its own side of the station**, which is the single property that keeps the four connector lines from crossing the station or each other, and which no screenshot review will catch once someone re-pairs a button; **a skin is paint** — the station layout signature is identical under every graphics style; and **the shipyard returns to the station with no second crossfade** (a replayed veil also swallows taps, so a returning player finds a dead screen — this shipped broken and was caught by driving the flow, not by reading it). All three of the visual/structural checks were negative-controlled: making the layout skin-dependent, re-pairing REST to a right-hand feature, and letting the neon skin fall through and paint nothing each fail exactly the intended check and nothing else — the fallen-through skin scores 8 lit samples against an honest 2023, so the bar at 300 is nowhere near vacuous. (Redo one with `.claude/scripts/negtest.sh save/restore` — these predate it and were restored by hand.) Six more pin the **routing** after the menu screen was removed: the game opens on pilot select and a pilot goes straight to its station (asserting `typeof showMenu === 'undefined'`, so a reintroduced menu fails loudly rather than stranding the player on a screen nothing returns to); REST banks a run and re-picking the pilot resumes score, hull, pip, shields and banked totals exactly; dying clears the run so the next pick starts fresh at the checkpoint; the death screen offers RETRY and PILOTS and no route to a menu; and **the stage banner stops painting off the play screen** — `stageBanner` only ticks down inside update()'s play branch, so off-play it freezes and keeps painting, which is how "STAGE 1 · WAVE 1" ended up sitting behind the SHIP DOWN overlay. The last two were negative-controlled through `negtest.sh`: removing the banner guard and making `restoreRun()` ignore the banked run each fail exactly their own check (banner: 563 gold pixels in the band either way when broken, 0 off-play when fixed). Seventeen more cover the **canvas title screen** added 2026-08-24, and they exist because that screen has no DOM at all: the word, the three pilot bays and the erase controls are drawn, so nothing about their geometry is checked by a browser. Most of them are assertions a layout engine would otherwise have made for free — bays on-screen, square, non-overlapping, each bin *below* its bay and not touching its neighbour — swept across six viewports including two landscape ones, because a title sized off a column count and bays sized off a viewport fraction can collide at one aspect ratio and be fine at every other. The ones that earn their place are the four a screenshot cannot see. **The dogfight stays in its arena over a minute of driven flight**: every single frame of a fight that has drifted into a far corner looks perfectly correct, and the first version of this check was itself *flaky* (~2 escaped samples in 18 000) until hard walls turned a soft bound into a real invariant — a gate that goes red one run in five is a gate that gets ignored. **It reads as three dimensions** — hulls foreshorten (`fs` floors at 0.24, tops out at 1.0) and depth swings sprite scale ~8×; stubbing `fs = 1` fails exactly this and nothing else. **Some passes come in front of the UI plane** (17-26% of samples), which is the property that lets a fighter cross the logo. **The fight actually resolves** — the floor is 2 kills a minute, not `> 0`, because `> 0` passed even when the guns fired down the nose and hit essentially nothing (98 shots, zero kills, over a minute). Two more pin things that ship silently broken: **the baked word re-bakes on a style switch** (a cache key that does not move with `gfx` leaves the old bitmap on screen forever) and **erasing the pilot you were flying clears the active handle**, since the next paint reads `saves[i].name` for the bay caption. **Erasing takes a deliberate hold** — a tap drains, a slide-off cancels — which matters because the `confirm()` dialog that used to guard it is gone. Six were negative-controlled through `negtest.sh`: an icon placed above its bay, a style-blind cache key, no screen containment, `fs` pinned to 1, a tap that erases, a lowercase glyph at cap height, and a title sized wider than the screen each fail exactly their own check. The erase check was *restructured* because of one of them: sharing a single pilot across its three stages read fine until stage 1 regressed, at which point the bay was empty, stage 2's press landed on the square instead of the bin, and the suite died on a null rather than reporting the failure it was there to catch — each stage re-seeds now. Six more cover the **3D MODELS ('model') style** added 2026-08-25, whose look depends on derived motion (bank) and a real per-face light. **A skin is paint, driven**: 90 frames of a pinned deterministic scenario (every randomly-rolled spawn field overwritten, fire timers parked) with real draws between the ticks leave the enemy sim byte-identical across all five styles, and **the painters leave no fingerprint on the sim objects** — bank state lives in a WeakMap, so `Object.keys` of every enemy is unchanged after painting. **Who banks is the spec**: the swaying drone banks, the straight-line shooter and tanker hold level *for free* (their measured lateral speed is zero), the spinner never touches the bank tracker at all; **the ship banks with its own motion** — right when flying right, left when left, decaying to level at rest (NaN-guarded so a dead derivation FAILS instead of throwing). **Banking changes the lighting, not just the outline**: the same half of the tanker painted at bank ±0.5 must swing dark-to-lit as the deck turns through the lamp (Δ > 40; ~180 measured). This check took three forms to get honest: left-vs-right in one frame passed on the per-face grime difference between the two side skirts with the lighting broken outright, and a 'rising flank is lit' reading had the geometry backwards — the deck, the biggest face, dominates the measurement and it brightens when banked TOWARD the light. Its negative control is a constant `lum`, which collapses the swing to ~30. A seventh (2026-08-25) pins the **station's model treatment**: an 8x8 box inside a hub panel must read as lit metal under model (601) and stay near-dark under neon (123) — bars at 350/200 sit mid-gap, because neon's ring-glow bleed keeps its floor well above black; killing sPanel's model branch drops the model reading to 111 and fails exactly this. And **the model dogfight projects real hulls where the sim says they are**, sampled only at moments a hull sits in front of the UI plane (the word paints over the far layer). All six were negative-controlled through `negtest.sh`: a sim branch reading `gfx`, bank written onto the entity, a dead `visBank`, the underside dim removed, a dead `dfDrawShipModel`, and a missing dropdown option each fail exactly their intended check(s) — and the dogfight-projection control caught its own check first: a flat 4-lit-pixel threshold passed on stray starfield pixels with the painter completely dead, so the bar is now 5% of the sample box. Four more (2026-08-25) cover the **3D ANIMLIGHT ('anim') style**, which shares that whole mesh renderer and adds three things of its own. **The scale-up is real and the framed contexts are exempt**: the same hull painted alone on a cleared canvas must measure 3x for the player and 2x for an ordinary enemy on the field, and exactly 1x in a pilot bay and in the baked logo — one check each, because they fail independently (dropping the `portrait` flag leaves the field check green and only the framed one red, which is precisely the bug that spills a 3x hull across its neighbouring bay). The boss's ceiling is 1.15 rather than 1.03 because its gun rim always has a muzzle glow or two alight and that glow legitimately reaches past the barrel tips (1.07 measured) — a bar that still cannot be cleared by an enemy-scale 2x, which is the mistake it exists to catch. **The rigs animate, in the place and on the schedule the spec claims.** The wingtip sensors are sampled in a box derived from ship-local coordinates so the check sits on the strobe rather than on a guessed pixel, and the assertion is *mostly dark with a short sharp swell* — a light that were simply left ON would clear any bare "is it bright" bar, and does fail this one (24 lit samples of 24 with `lAmp` stubbed to a constant). **The veins flow rather than breathe**: both ends of one conduit must brighten, and *not at the same time* — the phase gap between their peaks is the entire assertion, because a wavefront travelling down a polyline and a polyline pulsing as one look identical to every other measurement. Stubbing the wavefront position out takes the gap from 6 samples to 0 while leaving the swing at 54 either way. All four were negative-controlled through `negtest.sh`, and the two light checks are properly orthogonal: freezing `lAmp` fails the strobe and leaves the flow check green, killing the wavefront fails the flow and leaves the strobe green. Six more (2026-08-25) came with animlight becoming the DEFAULT and the shield and powerups being skinned per style. **Shields cap at 2 from every source that can grant one** — armor regen at tier 5, the S pickup pressed six times, and a run banked before the cap existed and clamped on restore. One check, three sources, because a cap added at only one of them is the whole failure mode; uncapping reports 5/7/9. **The powerup skins** are asserted by BEHAVIOUR rather than distinctness — a mesh-family pickup is a crate that tumbles, the flat ones hold still — and that rewrite was forced by its own negative control: with the mesh branch dead the four hashes still differed, because the letter was styled per style *outside* the branch. Distinct-hash checks are only as good as the region they hash. **The shield checks paint the shield ALONE onto the real backdrop**, which fixed a second green-for-the-wrong-reason: the first version sampled a box whose radius differs per style, so the sample moved between styles and could land on the hull, and it passed with every branch collapsed into neon. On the isolated canvas the profile is honest — a radial profile at 0.30R / 0.60R / 0.99R must climb monotonically and pile up at the limb, which is the CD's spherical-shell spec stated as a measurement; a flat gradient reads 161/161/145 and fails. **The sweep** uses the same shape as the vein-flow check and for the same reason: a shell that merely pulsed would brighten both flanks together, so the assertion is that the right flank peaks *later* than the left. And **the baked shield sprite re-bakes** on a style or charge change — the shells are baked because painting two radial-gradient discs live cost 13 frames in 169 over budget (baked: 0/169, better than the no-shield baseline), and a bake inherits the stale-bitmap trap, so the key is pinned directly. Six negative controls, each landing where it should: uncapping, a dead mesh-powerup branch, a shield collapsed to neon, a flat gradient (fails only the profile), a parked sweep, and a style-blind bake key (which the key check names precisely while the pixel checks show the collateral). Three more (2026-08-27) cover the **SPRITESHEETS ('sprite') style**, which pre-renders the model art into lazily-baked frames and flattens the title dogfight into a recorded track. **The cache actually fills** — a field frame under 'sprite' must mint ship + all-four-enemy-type entries in `SPR` (a dead dispatch branch falls back to live mesh painting and looks IDENTICAL, which is why pixel checks cannot catch it; killing the branch leaves 1 entry and fails exactly this). **Keys carry the stage hue and a stage change mints new frames** — a hue-blind key serves stage 1's green drones in the red sector forever with no error anywhere (dropping the hue from the key fails exactly this check and nothing else; both controls run through `negtest.sh`). And **the flattened dogfight records its full loop, keyed on the viewport** — drawing the title under 'sprite' must leave a 22s x 60Hz track whose key is the live `W|H|dpr`, because the track is recorded in screen space and a resize must re-record rather than replay the wrong geometry. Uses `page.evaluate` calls into the game's own module-level globals/functions directly (no `window.__GD`-style debug handle exists here). |
| `drive-ember-depths.cjs` | `games/ember-depths/` | 115 checks. The first group is the three things a screenshot cannot see. **Free look is durable**: until the player drags, the camera eases to keep the hero on screen; the first drag hands it over *for good*, and only the ⌖ button takes it back. The regression it guards is the camera quietly re-tethering on the next step — which is exactly what this game did by design until 2026-08-26, so a refactor that "restores" `followCam` reads as a fix and silently deletes the feature. **The relic panel is a read, never a turn**: open and close both go through `handleTap`, the same entry point that walks the hero, so the dismissing tap is aimed at a real walkable neighbour and the witness is `player.x/y` plus `turnCount` — `pathQueue.length` alone reads 0 both when nothing happened and when an adjacent tap already resolved. Its positive control fires the identical tap with the panel shut and requires the hero to move, so the check cannot pass on a hit-test that is simply broken. **×N means ×N**: one check drives all six stacking sites through the game's own `hurtPlayer` / `killEnemy` / `lightRadius`, because a site left on `relics.includes()` is the entire failure mode and per-relic checks invite the next relic to be forgotten. That one earned its shape from its own negative control — the first version re-derived `damage - relicCount('skin')` inside the check and passed with the shipping site reverted. Plus the zoom range, the four-edge clamp, zoom-1 layout identity, and a real two-finger spread through CDP `Input.dispatchTouchEvent` that must zoom the board without firing a tap or moving `visualViewport.scale` off 1. Five negative controls through `negtest.sh` (a `camFree`-blind `followCam`, a falling-through dismiss tap, skin/leech/ward each reverted to non-stacking), each failing exactly its own check. A sixth trap is recorded rather than guarded: the camera checks teleport the hero to drive `followCam` and can leave them standing in a wall, which made the panel group flaky at ~50% until each stage re-generated the floor. The second group covers the **meta layer** added 2026-08-26, and its shape is set by two rules. **A persistence check that does not reload has not checked persistence** — slot state lives in memory as well as in localStorage, so the three-independent-characters check writes, reloads the page, and only then reads. And **a save blob is untrusted input**: one check hands `migrate()` a hand-edited character (gear tier 99, a negative purse, a skill that does not exist, `lantern: 'x'`) and requires every field clamped into range, because an out-of-range gear tier indexes off the end of its track and throws mid-delve. Around those: the forge refusing a purchase you cannot afford, the kit **reaching the delve** (attack, max HP, light radius, gold multiplier and the marked stairs all read off what `startRun` leaves behind — gear that a shop can buy but a delve never sees is the failure mode), the skill gate and the rank-r-costs-r ladder, every skill effect measured through the game's own `hurtPlayer` / `killEnemy` / `playerAct` / `completeDescend` rather than re-derived, the supply round trip (bought at price → carried in → used → **a turn passes** → the leftovers lost with the body), and the payout, including that `checkDeath` being state-guarded is what stops one delve banking twice. Four more negative controls through `negtest.sh`: `bankRun` keeping the supplies, the skill gate removed, the kit never folded into `player`, and `migrate` trusting the blob — each failing exactly its own check. A third group came with the 2026-08-26 difficulty pass, and two of its checks were **green for the wrong reason first**. *A trap-avoidance check has to put the trap ON the route*: the first version planted it on any cell near the player, and "the path avoided it" was trivially true of a cell the path was never going to touch — it stayed green with trap-avoidance ripped out of the game entirely. It now builds the route with no trap, plants one on a cell that route uses, and asserts the detour; a second check asserts the documented fallback (a trap with no way round is walked, not refused). *And measure a rule by the thing it changes, not a proxy the layout can move*: "mired means the dark moves twice" was written as how far a tracked enemy closed in a turn, which reads 0 whenever the floor blocks it. Counting SWINGS from an enemy already beside you is layout-independent and is what the rule says. Around those: the skill DAG's wiring (every edge names a real node, exactly two roots, every node reachable by following edges forward), the prerequisite rule at full size — one complete side reaches its own capstone but NOT the final one, and both sides together cost exactly 16 points — respec, every trap kind and the status it leaves, TRAPWISE halving both, the milestone ledger (a point on floors 5 and 10 and nowhere else, and re-finishing pays nothing, because farming is for gold), and the sticky starting depth including that diving in deep and dying on arrival banks zero. Five more negative controls: an ANY-input prerequisite, a milestone paid every time, a depth bonus counted from floor 1, traps that never block the path, and venom that never expires. One later check is a **layout** check and earns its place by having caught a live defect during a refine pass: **no overlay screen may hide its own first row**. `#overlay` is a flex column that scrolls, and a centred one puts the first rows above the scroll origin where no gesture reaches them — which had shipped with the TITLE heading 9-24px out of reach in landscape, the first thing a player sees. It sweeps every screen (title, slots, all three camp tabs, a three-card relic offer, the death screen) across five viewports **portrait and landscape**, because the two clip on different screens: the camp overflows in portrait, the short ones only in landscape. Its negative control (restore `justify-content:center`) fails it on 11 screen/viewport pairs. A last lesson, from a flake this suite produced itself: **both halves of a generated-board fixture must come from the same floor.** The trap-path check regenerated the dungeon until it had found each of two candidate cells, so a detour found on floor 0 was asserted against floor 3's layout — flaky one run in five, and the flake read as a pathing bug rather than a test bug. The 2026-08-27 economy pass added two more: **trap pathing is now asserted the other way up** — the game stopped avoiding traps on purpose, so the check requires a trap planted on the route to leave that route byte-identical, and it keeps the *old* check's setup work (a cell that is on the route **and** has a detour) because that precondition is the entire discriminator; the naive flip is green with trap-blindness reverted, since a cell with no way round is crossed by any pathfinder. And **every gear and supply price in this suite is now read from `GEAR` / `CONSUMABLES` rather than restated** — five checks went red on the re-tune with `500 - 40 - 40` written into them, none of them because anything had broken. |
| `eval-ember-depths.cjs` | `games/ember-depths/` | Balance, not rules — and the only honest way to tune `CURVE`, whose interactions (enemy count × HP curve × the player's own kit) are not readable off the table. Scripted personas play REAL runs through `playerAct`/`useConsumable`/`takeRelic`, seeded via the game's `seedSalt` hook so a constant change compares curves rather than luck. It asserts the CD's spec as a shape: a fresh delver averages about depth 8, the first tier of gear and skills is felt, a finished build goes far deeper, and — the half no table can show — **the dark still wins in the end**, because "no ceiling" means the quadratic term has to beat any build eventually. Two things it is honest about rather than hiding. Its personas are scripted and play worse than a person, so the average is a FLOOR on what the CD will see, not a prediction, and the bands sit around measured values with room for noise rather than at the vacuous end. And a run that deadlocks or hits the depth cap is counted and printed in a `cut` column instead of being averaged in silently — an earlier persona re-derived its next step every turn from a BFS that routes around enemies, so a foe shuffling in a corridor flipped the gradient and the delver paced between two tiles for 6000 turns; that version reported "MAXED averages depth 3.5", which measured the deadlock, not the game. It now builds one path and follows it, the way a tap does. Rebuilt 2026-08-27 around **three skill bands** (novice / steady / veteran) and a second mode. The bands are what makes "does skill buy depth?" a check the eval can fail, and every difference between them is a **decision, never a stat**: whether you look where you are walking, whether you pick your ground, whether you buy on purpose. They must come out monotonic — fresh 5.9 / 7.2 / 7.5, kitted 14.5 / 16.5 / 17.7, maxed 34.7 / 35.0 / 44.6 — and getting there meant fixing three ways a tactical script measures itself instead of the game (hunting a pack rather than waiting for it; holding a chokepoint against archers, who never have to walk into it; re-choosing that chokepoint every turn, which makes a retreat a shuffle) plus one that had been quietly poisoning every number: **re-picking the goal whenever a path ran out** livelocked the delver between two equidistant unexplored corners — 200 consecutive moves, no enemies, no progress — and fixing it alone took truncation from 21 runs in 108 to 3. The second mode is `--campaign`: one character, N delves back to back, **shopping between them through the game's own `campAction`**, which is how the gear ladder is priced. Its number is **purchases per delve** (the CD's spec is ~1; measured 0.96 for all three personas over 25 delves), and it asserts both failure modes — nobody maxes the kit inside a short campaign, and a good player still climbs it. `--override '<json>'` sweeps candidate `CURVE` / `GEAR` values from the command line, skipping the assertions because the bands are calibrated to the shipping constants. Method: `.claude/notes/20260827-tuning-a-difficulty-curve-with-a-persona-eval.md` and `20260827-pricing-a-progression-economy.md`. |

Run them the same way as the smoke gate:

```
npm install playwright-core          # once, any directory
NODE_PATH=<that dir>/node_modules node .claude/tests/drive-wayfinder.cjs
```

Each prints a `... DRIVE: N passed, M failed` final line and exits 0/1.

Harness patterns, and the accumulated list of ways these tests go wrong before
the game does: `.claude/notes/20260724-headless-mobile-game-testing.md`.
